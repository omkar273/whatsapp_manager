import 'package:flutter/material.dart';

class ScheduledFragement extends StatelessWidget {
  const ScheduledFragement({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
